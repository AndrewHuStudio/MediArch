name: MediArch
services:
  postgres:
    image: postgres:15-alpine
    container_name: mediarch-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: mediarch_password_2024
      POSTGRES_DB: postgres
    ports:
      - "5432:5432"
    volumes:
      - E:/docker-data/mediarch/postgres:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - mediarch-network

  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: mediarch-api
    restart: unless-stopped
    env_file:
      - .env
    environment:
      ENVIRONMENT: production
      API_HOST: 0.0.0.0
      API_PORT: 8010
      CHECKPOINT_BACKEND: postgres
      STORE_BACKEND: postgres
      SESSION_STORE_BACKEND: postgres
      REQUIRE_POSTGRES_PERSISTENCE: "true"
      PERSISTENCE_POSTGRES_URI: postgresql://postgres:mediarch_password_2024@postgres:5432/postgres?sslmode=disable
      POSTGRES_CHECKPOINT_URI: postgresql://postgres:mediarch_password_2024@postgres:5432/postgres?sslmode=disable
      POSTGRES_STORE_URI: postgresql://postgres:mediarch_password_2024@postgres:5432/postgres?sslmode=disable
      POSTGRES_SESSION_STORE_URI: postgresql://postgres:mediarch_password_2024@postgres:5432/postgres?sslmode=disable
      NEO4J_URI: bolt://neo4j:7687
      MONGODB_URI: mongodb://admin:mediarch2024@mongodb:27017/
      MILVUS_HOST: milvus
      NEXT_PUBLIC_API_URL: http://localhost:8010
    depends_on:
      postgres:
        condition: service_healthy
      mongodb:
        condition: service_started
      milvus:
        condition: service_started
      neo4j:
        condition: service_started
    ports:
      - "8010:8010"
    networks:
      - mediarch-network

  mongodb:
    image: mongo:7.0
    container_name: mediarch-mongodb
    restart: unless-stopped
    ports:
      - "27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: mediarch2024
      MONGO_INITDB_DATABASE: mediarch
    volumes:
      - E:/docker-data/mediarch/mongodb:/data/db
      - E:/docker-data/mediarch/mongodb_config:/data/configdb
    networks:
      - mediarch-network

  etcd:
    image: quay.io/coreos/etcd:v3.5.15
    container_name: mediarch-etcd
    restart: unless-stopped
    environment:
      ETCD_AUTO_COMPACTION_MODE: revision
      ETCD_AUTO_COMPACTION_RETENTION: "1000"
      ETCD_QUOTA_BACKEND_BYTES: "8589934592"
      ETCD_SNAPSHOT_COUNT: "50000"
    volumes:
      - E:/docker-data/mediarch/etcd:/etcd
    command: >
      etcd -advertise-client-urls=http://127.0.0.1:2379
           -listen-client-urls http://0.0.0.0:2379
           --data-dir /etcd
    networks:
      - mediarch-network

  minio:
    image: minio/minio:RELEASE.2024-08-17T01-24-54Z
    container_name: mediarch-minio
    restart: unless-stopped
    environment:
      MINIO_ROOT_USER: minioadmin
      MINIO_ROOT_PASSWORD: minioadmin
      MINIO_BROWSER: "off"
    volumes:
      - E:/docker-data/mediarch/minio:/minio_data
    command: minio server /minio_data --console-address ":9001"
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:9000/minio/health/live"]
      interval: 30s
      timeout: 20s
      retries: 3
    networks:
      - mediarch-network

  milvus:
    image: milvusdb/milvus:v2.6.2
    container_name: mediarch-milvus
    restart: unless-stopped
    depends_on:
      - etcd
      - minio
    environment:
      MILVUS_DEPLOY_MODE: standalone
      ETCD_ENDPOINTS: etcd:2379
      MINIO_ADDRESS: minio:9000
      MINIO_ACCESS_KEY: minioadmin
      MINIO_SECRET_KEY: minioadmin
      DATA_COORD_SEGMENT_MAX_SIZE: "512"
      LOG_LEVEL: info
    volumes:
      - E:/docker-data/mediarch/milvus:/var/lib/milvus
    ports:
      - "19530:19530"
      - "9091:9091"
    command: ["milvus", "run", "standalone"]
    networks:
      - mediarch-network

  neo4j:
    image: neo4j:5.13-community
    container_name: mediarch-neo4j
    restart: unless-stopped
    ports:
      - "7474:7474"
      - "7687:7687"
    environment:
      NEO4J_AUTH: neo4j/mediarch2024
      NEO4J_PLUGINS: '["apoc"]'
      NEO4J_dbms_security_procedures_unrestricted: apoc.*
      NEO4J_dbms_security_procedures_allowlist: "apoc.*"
      NEO4J_server_memory_heap_max__size: 2G
    volumes:
      - E:/docker-data/mediarch/neo4j/data:/data
      - E:/docker-data/mediarch/neo4j/logs:/logs
    networks:
      - mediarch-network

networks:
  mediarch-network:
    driver: bridge
