# MediArch 前端环境变量配置

# 后端 API 地址
NEXT_PUBLIC_API_URL=http://localhost:8000

# 是否使用后端 API（false 则使用 Mock 模式，显示离线演示内容）
# 要启用离线演示模式，设置为 false
NEXT_PUBLIC_USE_BACKEND_API=true

# 是否启用调试模式
NEXT_PUBLIC_DEBUG=true
